<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600&display=swap" rel="stylesheet">

    <%
    title = file ? `Download ${file.name}` : 'File not found';
    seoTitle = file ? 'Something got beamed up for you!' : 'Whoops, nothing to see here.';
    seoDescription = file ? `Click this link to download "${file.name}" (${prettyBytes(file.size)})` : 'Contact your peer if you believe this is a mistake.';
    %>

    <!-- Theme color -->
    <meta name="theme-color" content="#3d7cf9"/>
    <meta name="msapplication-navbutton-color" content="#3d7cf9"/>
    <meta name="apple-mobile-web-app-status-bar-style" content="#3d7cf9"/>

    <!-- Open Graph / Facebook -->
    <meta property="og:image" content="/favicon.ico"/>
    <meta property="og:type" content="website"/>
    <meta property="og:url" content="https://beam.cafe"/>
    <meta property="og:site_name" content="Beam Cafe"/>
    <meta property="og:title" content="<%= seoTitle %>"/>
    <meta property="og:description" content="<%= seoDescription %>"/>

    <!-- Twitter -->
    <meta property="twitter:url" content="https://beam.cafe">
    <meta property="twitter:title" content="<%= seoTitle %>">
    <meta property="twitter:description" content="<%= seoDescription %>">
    <meta property="twitter:image" content="/favicon.ico">

    <title><%= title %></title>

    <!-- Icons -->
    <link rel="icon" href="/favicon.ico"/>
    <link rel="fluid-icon" href="/favicon.ico"/>
    <link rel="apple-touch-icon" href="/favicon.ico"/>
    <link rel="manifest" href="/manifest.json">
</head>
<body>

<% if(file) { %>
    <main>
        <h3><%= file.name %> (<%= prettyBytes(file.size) %>)</h3>
        <a href="/file/<%= file.id %>" target="_blank">Download File</a>
    </main>
<% } else { %>
    <main class="error">
        <p>File not found.</p>
        <p>Ask your peer if you believe this is a mistake.</p>
    </main>
<% } %>

<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    :root {
        --c-red: #ff252f;
        --c-red-accent: #e51f1f;

        --c-text-primary: #242930;
        --c-text-primary-accent: #4b515d;
        --c-text-secondary: #b6b9bf;
        --c-text-secondary-accent: #fff;
        --c-background-primary: #fff;
        --c-background-secondary: #e8e9eb;
        --c-primary: #3d7cf9;
        --c-primary-accent: #3071ea;
    }

    @media screen and (prefers-color-scheme: dark) {
        :root {
            --c-text-primary: #fafafa;
            --c-text-primary-accent: #dcddde;
            --c-text-secondary: #565a61;
            --c-background-primary: #2f3136;
            --c-background-secondary: #202225;
        }
    }

    html,
    body {
        height: 100%;
    }

    body {
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: 'Nunito', sans-serif;
        background: var(--c-background-secondary);
    }

    main {
        display: flex;
        flex-direction: column;
        padding: 1em 2em;
        border-radius: 0.5em;
        box-shadow: 0 0.2em 1em rgba(0, 0, 0, 0.15);
        background: var(--c-background-primary);
        max-width: 75vw;
    }

    main h3 {
        font-size: 1.25em;
        text-align: center;
        color: var(--c-text-primary);
        margin: 0.5em 0 1em;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        max-width: 20em;
    }

    main a {
        text-decoration: none;
        color: var(--c-text-primary);
        background: var(--c-primary);
        margin: 0 auto 0.5em;
        padding: 0.5em 3em;
        border-radius: 0.25em;
        transition: all 0.3s;
    }

    main a:hover {
        background: var(--c-primary-accent);
        color: var(--c-text-primary);
    }

    main.error {
        background: var(--c-red);
        text-align: center;
        line-height: 1.75em;
    }

    main.error p {
        color: var(--c-text-secondary-accent);
    }

</style>

</body>
</html>
